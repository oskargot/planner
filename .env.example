# API base for the sync server. Leave empty for same-origin (production),
# or point at the tailnet hostname when the dev server runs elsewhere.
# VITE_API_URL=https://jellybot.your-tailnet.ts.net
VITE_API_URL=
